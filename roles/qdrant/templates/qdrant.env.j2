# {{ ansible_managed }}
# Qdrant environment for instance: {{ qdrant_instance.name }}
# Permissions on this file are 0600 - do not share or commit.

QDRANT__SERVICE__API_KEY={{ qdrant_instance.api_key }}

{% if qdrant_instance.read_only_key is defined and qdrant_instance.read_only_key | length > 0 %}
QDRANT__SERVICE__READ_ONLY_API_KEY={{ qdrant_instance.read_only_key }}
{% endif %}
